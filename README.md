# team-21-bakeoff-
designing human centered software group projects

questions to ask prof:
- do they have to be using the trackpad the whole time?
- can u use the trackpad only to click? does it have to move?
- 

ideas for projects :
- thinking about how osu and other rythm games try to find the fastest way to submit 'input'
- maybe use trackpad to move + use keybind to snap onto the nearest dot (?)
- use keybinds to go thru the grid (like have q, w, e, r(for the column) + (# key for the row)
- ex: the dot is on the 1 column but 3rd row, you press 'q' + '3' at the same time to select the dot
- but you MUST use trackpad to click
- rotate or select based on keybinds but must use trackpad to click(?)
- use arrow keys too (?)
- color the boxes differently so it shows when the next attach
- 
